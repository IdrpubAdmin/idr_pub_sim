
toUpperCase() // 대문자로 바꿈

toLowerCase() // 소문자로 바꿈

indexOf() // 문자를 인수로 받아 몇번째 위치하는지 반환 (찾는 문자가 없으면 -1 반환)

slice(a,b) // 문자열 a부터 b까지 반환

substring(a,b) // a와 b 사이 문자열 반환

substr(a,n) // a부터 시작 n개를 반환

trim() // 앞뒤 공백제거

repeat(n) // 문자열 n번 반복

includes() // 문자를 인수로 받아 있으면 true 없으면 false를 반환

