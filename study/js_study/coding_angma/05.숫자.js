
Math.PI //3.1415926535...

Math.ceil() // 올림

Math.floor() // 내림

Math.round() // 반올림

Math.random() // 0~1사이 무작위 숫자 생성

Math.max() // 괄호안에 인수들중 최대값 반환

Math.min() // 괄호안에 인수들중 최소값 반환

Math.abs() // 괄호안의 수에 절대값을 구해줌

toFixed(2) // 소수점 둘째자릿수까지 반올림 (문자열 반환)

isNaN() // NaN인지 판별

parseInt() // 문자열을 숫자로 바꿔줌
Number() // 문자열을 숫자로 바꿔줌
// 두개의 차이점은 문자열이 섞여있을때 다름
// let margin = '10px'
// parseInt(margin) // 10
// Number(margin) // NaN

parseFloat() // 소수점까지 반환